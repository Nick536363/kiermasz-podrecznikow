# LISTING CRUD BACKEND

This is the backend of listing-manager app. It exposes API routes to the frontend part of this app. Backend uses `Fastify` as a web framework (server) and `Prisma ORM` as a ORM.

## Routes

| Route | Description        |
| ----- | ------------------ |
| `/`   | Simple hello world |
