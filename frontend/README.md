# LISTING-MANAGER-UI 1.0.0

This is the frontend of listing-manager app. It communicates with the API and displays information that it returns.

## .ENV

This file should look something like this:

```.env
VITE_API_URL="<URL_TO_API>"
```
